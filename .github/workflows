name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: 23f1002368@ds.study.iitm.ac.in
        run: echo "Hello, world! This is a test action."

      - name: Print Email
        run: echo "My email is 23f1002368@ds.study.iitm.ac.in"

